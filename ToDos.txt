Ilya - Keep
Benny - eMail

Keep - 
dynamic creator 
repear search - if todos / img
crudel



Email = 
1. search - filter
1.1 filter dropdown - read/unread
2. sort - date/subject 

3.side bar: (like navbar) nested?
buttons:
1. compose - book details 
2.inbox - open list cmp with buttons
3.sent - list of sent that were uploded from storage
4.trash - loaded from storage
5.starred - (onsale)

note to self : 
add a messegae to use that mail was sent.


//  const obj= {"a":"a", "b":"b"};
//  const newObj = {"a": obj.a, "b": obj.b}
//  const newObj1 = {...obj}


state = {
        noteToAdd: {
            type: 'txt',
            inputVal: '',
            title: ''
        }
        // noteToAdd2: {
        //     type: 'txt',
        //     inputVal: '',
        //     title: ''
        // }
    }
    // if (emailText) this.setState(...{ noteToAdd: { ...this.state.noteToAdd, inputVal: emailText }, noteToAdd2:{...this.state.noteToAdd2, inputVal: this.state.noteToAdd.inputVal } })
        if (emailText) this.setState({ noteToAdd: { ...this.state.noteToAdd, inputVal: emailText } })